// config/admin.js
// Store admin credentials (for development purposes only)
// In production, use environment variables instead of hardcoding

const admin = {
  email: 'admin@example.com',
  password: 'admin123'
};

export default admin;
